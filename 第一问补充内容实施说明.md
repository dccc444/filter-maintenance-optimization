# B题第一问补充内容实施说明

> **实施状态（2026-07-26）**：本说明所列补充内容已接入当前 `main` 工作流。实际实现针对原方案作了三项统计修正：维护效果终点要求连续 3 个可用观测点低于阈值；持续时间采用 Kaplan–Meier 和 60 日限制平均持续时间处理高比例右删失；维护前后下降率采用等长窗口并按设备聚类 Bootstrap。当前自动输出已增加 `maintenance_effect_duration*.csv`、`maintenance_effect_survival.csv`、`maintenance_slope_change*.csv`、图 9 和图 10。下文早期代码片段保留为设计过程记录，若与当前源码冲突，以 `src/problem1/analysis.py` 和测试为准。

## 一、文档目的

本文档用于补齐《B题分工.md》中第一问 A、B 两条工作线尚未完全闭环的内容，并将补充分析接入现有仓库的自动化流程。

仓库：

```text
https://github.com/dccc444/filter-maintenance-optimization
```

基准：

```text
main 分支
commit: 9035d41
```

本阶段只补充第一问，不进入第二问、第三问和第四问。

需要补充的核心内容为：

1. 在数据质量报告中明确重复时间戳检查结果；
2. 建立“维护效果有效持续时间”指标；
3. 建立“维护前后下降速度变化”指标；
4. 将新增结果接入分析、绘图、报告和测试流程；
5. 更新第一问分析报告；
6. 完成第一问最终封版。

---

# 二、当前第一问完成情况

现有第一问已经完成：

- 小时级透水率数据读取与清洗；
- 异常点标记；
- 日中位数聚合；
- 中维护、大维护记录匹配；
- 维护周期划分；
- 年周期和半年周期分析；
- 维护周期下降率估计；
- 维护反事实事件研究；
- 维护后上包络衰减分析；
- 设备级指标体系；
- 图表和第一问分析报告自动生成。

当前尚需补齐的内容为：

| 补充项目 | 当前状态 | 补充目标 |
|---|---|---|
| 重复记录检查 | 已计算但报告未显式展示 | 在数据质量表和文字中明确说明 |
| 维护效果保持时间 | 仅有第7、14、30日效果和30日保持率 | 给出维护效果持续多少天 |
| 维护后下降速度变化 | 有维护前斜率和维护后周期斜率 | 构造配对变化指标并统计检验 |

---

# 三、实施前准备

## 3.1 建立工作分支

在仓库根目录执行：

```powershell
git fetch origin
git switch -c ab/problem1-paper 9035d41
```

检查当前提交：

```powershell
git rev-parse --short HEAD
```

预期输出：

```text
9035d41
```

## 3.2 建立环境并复现基准

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

运行测试：

```powershell
.\.venv\Scripts\python.exe -m pytest
```

运行第一问：

```powershell
.\.venv\Scripts\python.exe -m src.problem1.run
```

保存一份基准输出：

```powershell
Copy-Item outputs\problem1 outputs\problem1_baseline -Recurse
```

---

# 四、补充一：重复时间戳检查说明

## 4.1 当前情况

现有 `build_quality_table()` 已经计算字段：

```text
duplicate_timestamps
```

当前10台设备该字段均为0，因此不需要额外进行重复观测删除或合并。

## 4.2 修改文件

修改：

```text
src/problem1/report.py
```

## 4.3 在数据质量表中增加字段

将数据质量表的字段列表改为：

```python
[
    "device",
    "rows",
    "valid_values",
    "missing_rate",
    "duplicate_timestamps",
    "calendar_hour_coverage",
    "calendar_day_coverage",
    "outliers_flagged",
    "minimum",
    "maximum",
]
```

## 4.4 增加文字说明

在数据质量说明中增加：

```text
经设备内时间戳重复检查，10台设备的重复时间戳数量均为0，
无需进行重复记录删除或合并。
```

## 4.5 验收标准

- 数据质量表中出现 `duplicate_timestamps`；
- 10台设备对应值均为0；
- 第一问报告明确写出重复值检查结论。

---

# 五、补充二：维护效果有效持续时间

## 5.1 分析目的

当前第一问已经计算：

- 维护后1—3日反事实恢复量；
- 第7日维护效果；
- 第14日维护效果；
- 第30日维护效果；
- 第30日保持率。

这些指标只能回答维护效果在若干固定时点的剩余程度，尚不能直接回答：

> 一次维护的效果能够维持多少天？

因此需要增加维护效果有效持续时间。

## 5.2 数学定义

对第 \(k\) 次维护，设维护后第 \(t\) 日实际透水率相对于无维护反事实轨迹的增量为：

\[
G_k(t).
\]

定义维护后的初始恢复量为：

\[
G_{0,k}
=
\frac{1}{3}
\sum_{t=1}^{3}G_k(t).
\]

为减小单日波动影响，对维护效果做5日滚动中位数平滑：

\[
\widetilde G_k(t)
=
\operatorname{Median}
\left\{
G_k(t-2),\ldots,G_k(t+2)
\right\}.
\]

设效果终止阈值为初始恢复量的20%：

\[
G_{\mathrm{thr},k}
=
0.2G_{0,k}.
\]

维护效果有效持续时间定义为：

\[
T_{\mathrm{eff},k}
=
\min
\left\{
t\geq4:
\widetilde G_k(t)
\leq0.2G_{0,k}
\right\}.
\]

含义：

> 当维护带来的透水率额外提升下降到初始恢复量的20%以下时，认为本次维护的主要效果结束。

## 5.3 为什么要处理右删失

一次维护之后可能很快发生下一次维护，或者数据在维护效果结束前已经停止记录。

因此，单次维护的最大观察时间必须设置为：

\[
T_{\max}
=
\min
\left\{
90,\;
\text{距下一次维护天数}-1,\;
\text{距数据结束天数}
\right\}.
\]

如果截至 \(T_{\max}\)，维护效果仍未下降到阈值，则不能把 \(T_{\max}\) 当作真实持续时间，只能记录：

```text
duration_censored = True
```

含义：

> 当前数据只能说明维护效果至少持续到该时点，实际持续时间可能更长。

## 5.4 新增函数

在：

```text
src/problem1/analysis.py
```

中增加：

```python
def estimate_effect_duration(
    curve: pd.DataFrame,
    initial_gain: float,
    threshold_ratio: float = 0.2,
    smooth_window: int = 5,
) -> dict:
    # 估计一次维护事件的有效持续时间
    post = (
        curve.loc[curve["relative_day"] >= 1]
        .sort_values("relative_day")
        .copy()
    )

    followup_days = (
        int(post["relative_day"].max())
        if not post.empty
        else 0
    )

    if not np.isfinite(initial_gain) or initial_gain <= 0:
        return {
            "effective_duration_days": np.nan,
            "duration_censored": False,
            "duration_status": "nonpositive_initial_gain",
            "duration_threshold": np.nan,
            "followup_days": followup_days,
        }

    threshold = threshold_ratio * initial_gain

    post["effect_smooth"] = (
        post["effect"]
        .rolling(
            window=smooth_window,
            center=True,
            min_periods=3,
        )
        .median()
    )

    crossing = post.loc[
        (post["relative_day"] >= 4)
        & (post["effect_smooth"] <= threshold)
    ]

    if not crossing.empty:
        duration = int(crossing.iloc[0]["relative_day"])
        censored = False
        status = "threshold_crossed"
    else:
        duration = followup_days
        censored = True
        status = "right_censored"

    return {
        "effective_duration_days": duration,
        "duration_censored": censored,
        "duration_status": status,
        "duration_threshold": threshold,
        "followup_days": followup_days,
    }
```

## 5.5 修改事件研究观察窗口

当前事件研究默认只观察维护后30日，需要扩展到最多90日，但不能跨越下一次维护。

在 `event_study()` 中先增加下一次维护日期：

```python
events = maintenance.sort_values(
    ["device", "date"]
).copy()

events["next_event_date"] = (
    events.groupby("device")["date"].shift(-1)
)
```

对每次维护计算最大观察天数：

```python
device_daily = season_frame.loc[
    season_frame["device"] == event["device"]
]

data_end = device_daily["date"].max()
days_to_end = int((data_end - event["date"]).days)

if pd.notna(event["next_event_date"]):
    days_to_next = int(
        (event["next_event_date"] - event["date"]).days
    ) - 1
else:
    days_to_next = days_to_end

max_day = max(
    0,
    min(90, days_to_end, days_to_next),
)
```

然后调用：

```python
result = _event_effects_for_one(
    device_daily,
    event["date"],
    max_day=max_day,
)
```

获得事件曲线后，再调用：

```python
duration_metrics = estimate_effect_duration(
    curve,
    initial_gain=record["counterfactual_gain_3d"],
)
record.update(duration_metrics)
```

## 5.6 需要生成的结果表

事件级表：

```text
outputs/problem1/tables/maintenance_effect_duration.csv
```

至少包含：

```text
event_id
device
event_date
maintenance_type
counterfactual_gain_3d
effective_duration_days
duration_censored
duration_status
duration_threshold
followup_days
```

汇总表：

```text
outputs/problem1/tables/maintenance_effect_duration_summary.csv
```

至少包含：

```text
maintenance_type
n_events
valid_duration_events
right_censored_events
right_censored_share
median_duration_uncensored
q25_duration_uncensored
q75_duration_uncensored
```

注意：

- 非正初始增益事件不能进入正常持续时间统计；
- 右删失事件不能当作精确持续时间参与普通中位数；
- 报告必须同时说明右删失比例。

## 5.7 建议新增绘图

新增：

```text
outputs/problem1/figures/09_维护效果持续时间.png
```

建议画法：

- 横轴：中维护、大维护；
- 纵轴：有效持续时间（天）；
- 箱线图表示未删失事件；
- 散点表示每次维护；
- 右删失事件使用不同点形；
- 图注中说明删失点表示“至少持续至该日”。

---

# 六、补充三：维护前后下降速度变化

## 6.1 分析目的

现有结果已经包括：

- 维护前21日局部趋势斜率 `pre_slope_per_day`；
- 维护后完整周期下降率 `decline_rate_per_day`。

因此可以进一步分析：

> 维护除了提高透水率水平之外，是否会改变后续下降速度？

## 6.2 指标定义

维护前下降率定义为：

\[
DR_{\mathrm{pre},k}
=
-\beta_{\mathrm{pre},k}.
\]

维护后下降率定义为：

\[
DR_{\mathrm{post},k}
=
-\beta_{\mathrm{post},k}.
\]

维护前后下降率变化定义为：

\[
\Delta DR_k
=
DR_{\mathrm{post},k}
-
DR_{\mathrm{pre},k}.
\]

解释：

- \(\Delta DR_k>0\)：维护后下降速度更快；
- \(\Delta DR_k<0\)：维护后下降速度更慢；
- \(\Delta DR_k\approx0\)：维护主要改变透水率水平，对下降速度影响不明显。

## 6.3 维护事件和后续周期的对应关系

观测起点对应：

```text
cycle_id = 0
```

第一次维护后对应：

```text
cycle_id = 1
```

第二次维护后对应：

```text
cycle_id = 2
```

依此类推。

在：

```text
src/problem1/analysis.py
```

中增加：

```python
def build_maintenance_slope_change(
    maintenance: pd.DataFrame,
    event_metrics: pd.DataFrame,
    cycle_slopes: pd.DataFrame,
) -> pd.DataFrame:
    events = (
        maintenance.reset_index(names="event_id")
        .sort_values(["device", "date"])
        .copy()
    )

    events["cycle_id"] = (
        events.groupby("device").cumcount() + 1
    )

    paired = event_metrics.merge(
        events[
            [
                "event_id",
                "device",
                "date",
                "maintenance_type",
                "cycle_id",
            ]
        ],
        on=[
            "event_id",
            "device",
            "maintenance_type",
        ],
        how="left",
    )

    paired = paired.merge(
        cycle_slopes[
            [
                "device",
                "cycle_id",
                "decline_rate_per_day",
                "n_observed_days",
                "span_days",
            ]
        ],
        on=["device", "cycle_id"],
        how="left",
    )

    paired["pre_decline_rate"] = (
        -paired["pre_slope_per_day"]
    )

    paired["post_decline_rate"] = (
        paired["decline_rate_per_day"]
    )

    paired["decline_rate_change"] = (
        paired["post_decline_rate"]
        - paired["pre_decline_rate"]
    )

    return paired
```

注意检查合并后的日期字段命名，避免产生不必要的 `date_x` 和 `date_y`。

## 6.4 统计汇总

对中维护和大维护分别计算：

- 有效事件数；
- 维护前下降率中位数；
- 维护后下降率中位数；
- 变化量中位数；
- 变化量均值；
- Bootstrap 95%区间；
- Wilcoxon配对符号秩检验；
- 维护后下降加快事件比例。

增加：

```python
def summarize_slope_changes(
    frame: pd.DataFrame,
) -> pd.DataFrame:
    rng = np.random.default_rng(RANDOM_SEED)
    records = []

    for maintenance_type in MAINTENANCE_ORDER:
        subset = frame.loc[
            frame["maintenance_type"] == maintenance_type
        ].dropna(
            subset=[
                "pre_decline_rate",
                "post_decline_rate",
                "decline_rate_change",
            ]
        )

        values = subset[
            "decline_rate_change"
        ].to_numpy(dtype=float)

        if len(values) == 0:
            continue

        boot = rng.choice(
            values,
            size=(2000, len(values)),
            replace=True,
        )

        boot_median = np.median(boot, axis=1)

        nonzero = values[
            np.abs(values) > 1e-12
        ]

        if len(nonzero):
            test = stats.wilcoxon(nonzero)
            p_value = float(test.pvalue)
        else:
            p_value = 1.0

        records.append(
            {
                "maintenance_type": maintenance_type,
                "n_events": len(values),
                "pre_decline_median": float(
                    subset["pre_decline_rate"].median()
                ),
                "post_decline_median": float(
                    subset["post_decline_rate"].median()
                ),
                "median_change": float(
                    np.median(values)
                ),
                "mean_change": float(
                    np.mean(values)
                ),
                "ci95_low": float(
                    np.quantile(boot_median, 0.025)
                ),
                "ci95_high": float(
                    np.quantile(boot_median, 0.975)
                ),
                "wilcoxon_p_value": p_value,
                "faster_share": float(
                    np.mean(values > 0)
                ),
            }
        )

    return pd.DataFrame(records)
```

## 6.5 需要生成的结果表

事件级表：

```text
outputs/problem1/tables/maintenance_slope_change.csv
```

字段至少包括：

```text
device
event_date
maintenance_type
cycle_id
pre_decline_rate
post_decline_rate
decline_rate_change
n_observed_days
span_days
```

汇总表：

```text
outputs/problem1/tables/maintenance_slope_change_summary.csv
```

## 6.6 建议新增绘图

新增：

```text
outputs/problem1/figures/10_维护前后下降速度变化.png
```

建议画法：

- 横轴：维护类型；
- 纵轴：\(\Delta DR\)；
- 箱线图；
- 叠加事件散点；
- 添加 \(y=0\) 水平虚线。

解释：

- 点位于0以上：维护后下降加快；
- 点位于0以下：维护后下降减缓。

---

# 七、将新增结果接入现有自动流程

## 7.1 修改 `AnalysisResults`

在 `src/problem1/analysis.py` 的 `AnalysisResults` 中增加：

```python
maintenance_duration: pd.DataFrame
maintenance_duration_summary: pd.DataFrame
maintenance_slope_change: pd.DataFrame
maintenance_slope_change_summary: pd.DataFrame
```

## 7.2 修改 `run_analysis()`

在事件研究和周期下降率计算完成后增加：

```python
maintenance_duration = (
    event_metrics[
        [
            "event_id",
            "device",
            "event_date",
            "maintenance_type",
            "counterfactual_gain_3d",
            "effective_duration_days",
            "duration_censored",
            "duration_status",
            "duration_threshold",
            "followup_days",
        ]
    ].copy()
)

duration_summary = summarize_effect_duration(
    maintenance_duration
)

slope_change = build_maintenance_slope_change(
    maintenance,
    event_metrics,
    cycle_slopes,
)

slope_change_summary = summarize_slope_changes(
    slope_change
)
```

并将4项加入 `AnalysisResults(...)` 返回值。

## 7.3 修改 `run.py`

在 `table_map` 中增加：

```python
"maintenance_effect_duration.csv":
    results.maintenance_duration,

"maintenance_effect_duration_summary.csv":
    results.maintenance_duration_summary,

"maintenance_slope_change.csv":
    results.maintenance_slope_change,

"maintenance_slope_change_summary.csv":
    results.maintenance_slope_change_summary,
```

## 7.4 修改 `plots.py`

新增函数：

```python
plot_maintenance_duration(...)
plot_maintenance_slope_change(...)
```

并在 `make_all_plots()` 中增加：

```python
plot_maintenance_duration(results, output_dir)
plot_maintenance_slope_change(results, output_dir)
```

## 7.5 修改 `report.py`

在“中维护和大维护的影响”部分增加：

```markdown
### 维护效果持续时间

### 维护前后下降速度变化
```

报告中至少说明：

- 中维护持续时间结果；
- 大维护持续时间结果；
- 有效事件数量；
- 右删失比例；
- 维护前后下降率变化；
- Bootstrap区间或Wilcoxon结果；
- 不能进行因果解释的限制。

---

# 八、报告中的规范表述

## 8.1 维护持续时间部分

建议写法：

> 为衡量维护效果的持续性，本文将维护后1—3日的平均反事实增益作为初始恢复量，并将维护效果经5日滚动中位数平滑后首次下降至初始恢复量20%的时点定义为有效持续时间。考虑到下一次维护或观测期结束可能使维护效果终点无法被完整观测，本文将此类事件作为右删失事件处理，不将其观测末日直接解释为真实持续时间。

## 8.2 维护后下降速度部分

建议写法：

> 本文将维护前21日局部反事实趋势的下降率与维护后完整运行周期的稳健下降率进行配对比较，并以两者之差衡量维护后下降速度变化。该分析能够反映维护前后经验下降规律的变化，但由于维护类型并非随机分配，且大维护往往在设备状态较差时实施，因此结果仅作关联性解释，不直接视为维护对长期退化速度的因果效应。

## 8.3 禁止使用的表述

不建议直接写：

```text
大维护会损坏过滤器。
```

不建议直接写：

```text
大维护的效果比中维护差。
```

不建议直接写：

```text
维护后下降率增加，证明维护会加速老化。
```

更合理的表述是：

```text
样本中维护后的经验下降率出现变化，但该变化同时可能受到维护时点、
设备状态、季节负荷和未记录小维护等因素影响。
```

---

# 九、补充自动化测试

修改：

```text
tests/test_problem1.py
```

## 9.1 测试维护效果不衰减时为右删失

```python
def test_effect_duration_is_right_censored():
    curve = pd.DataFrame(
        {
            "relative_day": np.arange(1, 61),
            "effect": np.full(60, 15.0),
        }
    )

    result = estimate_effect_duration(
        curve,
        initial_gain=15.0,
    )

    assert result["duration_censored"]
    assert result["effective_duration_days"] == 60
```

## 9.2 测试指数衰减时能够找到交点

```python
def test_effect_duration_detects_decay():
    days = np.arange(1, 61)
    effect = 20 * np.exp(-days / 20)

    curve = pd.DataFrame(
        {
            "relative_day": days,
            "effect": effect,
        }
    )

    result = estimate_effect_duration(
        curve,
        initial_gain=20.0,
    )

    assert not result["duration_censored"]
    assert 29 <= result[
        "effective_duration_days"
    ] <= 36
```

## 9.3 测试维护事件与后续周期匹配

构造两次维护及两个维护后周期，检查：

```python
assert result.loc[0, "cycle_id"] == 1
assert result.loc[1, "cycle_id"] == 2
```

---

# 十、重新生成第一问结果

执行：

```powershell
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m src.problem1.run
```

检查：

```powershell
Get-ChildItem outputs\problem1\tables
Get-ChildItem outputs\problem1\figures
```

必须新增：

```text
maintenance_effect_duration.csv
maintenance_effect_duration_summary.csv
maintenance_slope_change.csv
maintenance_slope_change_summary.csv
09_维护效果持续时间.png
10_维护前后下降速度变化.png
```

---

# 十一、结果一致性检查

## 11.1 样本量检查

核对以下数量是否一致：

- 维护记录总数；
- 中维护事件数；
- 大维护事件数；
- 持续时间有效事件数；
- 右删失事件数；
- 斜率变化有效事件数。

## 11.2 持续时间检查

确认：

- 没有跨越下一次维护；
- 没有超过数据结束日期；
- 非正初始恢复量未进入正常持续时间统计；
- 右删失事件没有当成精确持续时间；
- 第30日以后观测使用的都是实际数据。

## 11.3 下降率变化检查

确认：

```text
pre_decline_rate = -pre_slope_per_day
```

确认：

- 第一次维护对应 `cycle_id=1`；
- 第二次维护对应 `cycle_id=2`；
- 维护前后下降率单位一致；
- 中维护、大维护分别汇总；
- 报告未进行过度因果解释。

## 11.4 图表检查

确认：

- 中文字体正常；
- 图9、图10无乱码；
- 中维护和大维护颜色与现有图一致；
- 单位明确；
- 所有图可通过一次运行自动生成。

---

# 十二、更新README和第一问报告

## 12.1 README新增内容

在第一问已实现功能中增加：

```text
- 维护效果有效持续时间估计，并处理下一次维护造成的右删失；
- 维护前后下降速度的配对比较与统计检验。
```

在主要输出中增加新表和新图说明。

## 12.2 第一问报告结构调整

建议将原“中维护和大维护的影响”扩展为：

```text
5. 中维护和大维护的影响
5.1 短期恢复量
5.2 维护效果保持与有效持续时间
5.3 维护前后下降速度变化
```

## 12.3 第一问指标体系调整

原指标体系保留，同时新增：

8. **维护有效持续时间 \(T_{\mathrm{eff}}\)**：维护效果下降至初始恢复量20%所需天数；

9. **下降率变化 \(\Delta DR\)**：维护后周期下降率减去维护前局部下降率。

---

# 十三、最终验收标准

第一问补充工作完成后，应满足：

## 数据分析

- [ ] 重复记录检查结果已明确；
- [ ] 有维护效果持续时间定义；
- [ ] 正确处理右删失；
- [ ] 有维护前后下降率变化指标；
- [ ] 中维护、大维护分别汇总；
- [ ] 有置信区间或统计检验；
- [ ] 未进行不合理因果推断。

## 程序

- [ ] 新增结果接入 `AnalysisResults`；
- [ ] 新增表接入 `run.py`；
- [ ] 新增图接入 `plots.py`；
- [ ] 新增文字接入 `report.py`；
- [ ] 新增测试全部通过；
- [ ] 一条命令可生成全部结果。

## 输出文件

- [ ] `maintenance_effect_duration.csv`；
- [ ] `maintenance_effect_duration_summary.csv`；
- [ ] `maintenance_slope_change.csv`；
- [ ] `maintenance_slope_change_summary.csv`；
- [ ] `09_维护效果持续时间.png`；
- [ ] `10_维护前后下降速度变化.png`；
- [ ] 更新后的 `第一问分析报告.md`。

## 文档

- [ ] README已更新；
- [ ] 第一问报告数值与CSV一致；
- [ ] 图表编号连续；
- [ ] 第一问已经完整覆盖《B题分工.md》2.1和2.2。

---

# 十四、提交建议

完成并检查后执行：

```powershell
git status
git add src tests outputs README.md
git commit -m "feat(problem1): complete duration and slope-change analysis"
```

建议打标签：

```powershell
git tag problem1-final
```

本提交完成后，第一问正式封版，不再随意修改第一问的数据口径和核心指标定义。
